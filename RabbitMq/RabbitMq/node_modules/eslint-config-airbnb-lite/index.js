var config = require('eslint-config-airbnb')

config.plugins = []
delete config.ecmaFeatures.jsx
for (var rule in config.rules) {
  if (rule.indexOf('react/') === 0) {
    delete config.rules[rule]
  }
}

module.exports = config
